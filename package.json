{
  "name": "feedback-loop",
  "version": "0.1.0",
  "description": "Turn chat feedback into reviewed, verified pull requests — and stop at the merge button.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "feedback-loop": "./bin/feedback-loop.mjs"
  },
  "scripts": {
    "fl": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "tsx --test \"src/**/*.test.ts\""
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.126.0",
    "yaml": "^2.9.1",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2"
  }
}
